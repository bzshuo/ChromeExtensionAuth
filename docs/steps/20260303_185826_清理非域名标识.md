## 本次需求

- 清理项目中除域名配置外包含 `jiangtai` 的标识信息（Native 主机名、目录名、日志名等），避免暴露不必要的命名信息，同时保证功能不受影响。

## 本次改动

- 更新 `background.js`：
  - 将 `NATIVE_HOST_NAME` 从 `com.jiangtai.auth_capture` 改为 `com.auth_capture.bridge`，仅影响与 Native 主机通信的名称，不改动任何域名常量（`*.jiangtai.com` 保持不变）。
- 更新 `popup.js`：
  - 将弹窗里使用的 `NATIVE_HOST_NAME` 同步改为 `com.auth_capture.bridge`，与后台脚本保持一致。
- 更新 `native_host/install_mac.sh`：
  - 将安装目录由 `jiangtai_auth_host` 改为 `auth_capture_host`。
  - 将生成的 manifest 文件名改为 `com.auth_capture.bridge.json`，manifest 内的 `"name"` 改为 `com.auth_capture.bridge`。
- 更新 `native_host/run.sh`：
  - 将日志文件路径从 `/tmp/native_host_jiangtai_debug.log` 改为 `/tmp/native_host_debug.log`，并同步更新 `LOG` 变量和 `NATIVE_HOST_DEBUG_LOG` 环境变量使用的路径。
- 更新步骤文档：
  - `20260303_120827_安装到纯英文路径.md`：将描述中的日志路径改为 `/tmp/native_host_debug.log`，后续可清理其中示例中的旧子目录名说明。
  - `20260303_115923_日志改为tmp并排查manifest.md`：将日志路径说明更新为 `/tmp/native_host_debug.log`，并将示例中 `com.jiangtai.auth_capture.json` 改为 `com.auth_capture.bridge.json`。

## 后续可选清理

- 目前剩余的 `jiangtai` 字符串主要保留在：
  - 域名配置（如 `https://med-pc-staff-dev.jiangtai.com`），这是业务必需，保持不变。
  - 若干历史步骤文档中的项目路径示例（如 `chrome-extension-jiangtai-auth`），仅作为本地路径说明，不影响运行，可按需后续再做重命名。

